// Защита отключена — разрешаем прямой доступ
// Раскомментируйте строки ниже, чтобы включить проверку авторизации

// if (localStorage.getItem('access') !== 'true') {
//   location.href = 'index.html';
// }
